# 现有的设计问题
- 窗体类（指如 LoginWin 的类）构建函数的职责是否过重？（目前重的原因是我们希望将尽可能多的运行时条件设计为类不变式（比如 UiWin 必须创建成功，否则整个窗体对象就会构造失败）这样在其余函数就不需检查其有效性）
- Ws, AcliveBackendClient 并非线程安全。如果 `co_await connectAsync` 同时在协程之外（如按钮响应函数）调用 AcliveBackendClient 的其他成员函数，就会产生竞态条件。我们需要通过类型系统或语法来确保不会（或难以）写出这样的代码。
